import "@/ui/main";
import start from "@/canvas/main";

start();
/**
    standard workflow for the project.
    1. build ui
        1. check if the webgl2 is availabe or not, if not then throw error.
    2. if webgl was available the canvas will be created
*/

/**
 */

/**
    A standard threeJS project contain
    - renderer
    - camera
    - scene
        - mesh
            - geometry
            - material
        - light
        - camera
*/
